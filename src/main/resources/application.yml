spring:
  threads:
    virtual:
      enabled: true
  profiles:
    active: dev

  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false

  data:
    redis:
      repositories:
        enabled: false